# Copy this file to .env and edit before running docker-compose up
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme
LOG_LEVEL=INFO
APP_ENV=production
# HOST_IP is used in Prometheus scrape configs when running outside Docker
HOST_IP=172.17.0.1
